{
  "name": "gns-ui-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Genesis UI - HELIX WebUI design system (React + Vite + Tailwind v4)",
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @gns/catalogue dev",
    "build": "bun run --filter @gns/catalogue build && bun run --filter @gns/harness build",
    "build:harness": "bun run --filter @gns/harness build",
    "watch:harness": "bun run --filter @gns/harness watch",
    "typecheck": "tsc -p packages/ui && tsc -p packages/helix && tsc -p packages/catalogue && tsc -p packages/harness"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^6",
    "tailwindcss": "^4.3.3",
    "typescript": "^7",
    "vite": "^8"
  }
}
